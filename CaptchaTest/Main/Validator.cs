
namespace CaptchaTest
{
    public static partial class Validator
    {
        //public static Validation GasBillInquiry(GasBillInquiryInput ws)
        //{
        //    //var info = Check(ws);
        //    //if (info.Result == false)
        //    //    return info;

        //    //info = AAA(ws?.Identity?.JsonWebToken);
        //    //if (info.Result == false)
        //    //    return info;

        //    var request = new GasBillInquiry
        //    {
        //        SearchFields = new GasBillInquirySearchFields { GasParticipateCode = ws.Parameters.ParticipateCode.ToEnglishNumber() }
        //    };

        //    var status = request.SearchFields.DoesGasParticipateCodeValid();
        //    if (status == true)
        //    {
        //        if (request.SearchFields.GasParticipateCodeIsValid == false)
        //            return new Validation { Result = false, Reason = request.SearchFields.Reason };
        //    }
        //    else
        //        return new Validation { Result = false, Reason = request.SearchFields.Reason };

        //    return new Validation { Result = true, Attachment = request };
        //}
        //public static Validation EncryptGasBillInquiryRequest(EncryptGasBillInquiryRequestInput ws)
        //{
        //    //var info = Check(ws);
        //    //if (info.Result == false)
        //    //    return info;

        //    //info = AAA(ws?.Identity?.JsonWebToken);
        //    //if (info.Result == false)
        //    //    return info;

        //    var request = new GasBillInquiry
        //    {
        //        SearchFields = new GasBillInquirySearchFields { GasParticipateCode = ws.Parameters.ParticipateCode.ToEnglishNumber() }
        //    };

        //    var status = request.SearchFields.DoesGasParticipateCodeValid();
        //    if (status == true)
        //    {
        //        if (request.SearchFields.GasParticipateCodeIsValid == false)
        //            return new Validation { Result = false, Reason = request.SearchFields.Reason };
        //    }
        //    else
        //        return new Validation { Result = false, Reason = request.SearchFields.Reason };

        //    return new Validation { Result = true, Attachment = request };
        //}
        //public static Validation DecryptGasBillInquiryResponse(DecryptGasBillInquiryResponseInput ws)
        //{
        //    //var info = Check(ws);
        //    //if (info.Result == false)
        //    //    return info;

        //    //info = AAA(ws?.Identity?.JsonWebToken);
        //    //if (info.Result == false)
        //    //    return info;

        //    var request = new GasBillInquiry
        //    {
        //        ResponseString = ws.Parameters.ResponseString,
        //        SearchFields = new GasBillInquirySearchFields { GasParticipateCode = ws.Parameters.ParticipateCode.ToEnglishNumber() }
        //    };

        //    var status = request.SearchFields.DoesGasParticipateCodeValid();
        //    if (status == true)
        //    {
        //        if (request.SearchFields.GasParticipateCodeIsValid == false)
        //            return new Validation { Result = false, Reason = request.SearchFields.Reason };
        //    }
        //    else
        //        return new Validation { Result = false, Reason = request.SearchFields.Reason };

        //    return new Validation { Result = true, Attachment = request };
        //}
    }
}